bool true_vowel(char symbol) {
    if ((symbol == 'a') || (symbol == 'e') || (symbol == 'y') || (symbol == 'u') || (symbol == 'i') || (symbol == 'o'))
    {
            return true;
    } else {
            return false;
    }
}